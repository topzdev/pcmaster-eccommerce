import React, { Fragment } from "react";
import { Link } from "react-router-dom";
import _ from "lodash";
const MenuDropdown = ({ list }) => {
    const { items } = list;
    return <div className="menu__dropdown"></div>;
};

export default MenuDropdown;
